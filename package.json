{
  "name": "montage",
  "version": "0.0.1",
  "description": "montage",
  "main": "dist/index.html",
  "repository": {
    "url": "git+ssh://git@github.com/NGS2Montage/montage.git",
    "type": "git"
  },
  "author": "Biocomplexity",
  "license": "MIT",
  "engines": {
    "node": "8.6.0",
    "npm": "5.4.2",
    "yarn": "1.1.0"
  },
  "scripts": {
    "start": "nps",
    "eslint-fix": "./node_modules/eslint/bin/eslint.js --fix .",
    "preinstall": "npm install -g npm && npm install -g yarn",
    "cleaninstall": "rm -rf bower_components && rm -rf yarn.lock && rm -rf node_modules && yarn cache clean && yarn install",
  },
  "dependencies": {
    "pushstate-server": "^3.0.0",
  },
  "devDependencies": {
    "babel-eslint": "^7.2.3",
    "babel-plugin-istanbul": "^4.1.4",
    "eslint": "^3.19.0",
    "html-hint": "^0.2.4",
    "stylelint": "^8.1.1",
    "stylelint-config-standard": "^17.0.0"
  }
}
